# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'

- script: |  # Multi-line script for clarity
    git config --global user.name 'AmirHome'  # Replace with your username
    git config --global user.email 'amir.email@yahoo.com'  # Replace with your email
    git remote add github https://github.com/AmirHome/patients_v2.git
    # cat .git/config
    # git push -u github master
    # git push github --mirror  # Push all branches to backup remote
    git push https://amir.email%40yahoo.com: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@github.com/AmirHome/patients_v2.git master
